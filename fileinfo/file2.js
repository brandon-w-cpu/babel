class MyClass {
	
  constructor(a, b, c, d) {
    e = a; 
    this.b = b; 
    this.c = c; 
    this.d = d; 
  }

  subtract(a, b, c) {
    return a - b
  }

  add(a, b) {
    return b + a; 
  }

  returnNothing(a, b) {
    console.log(a + b); 
  }
    
}